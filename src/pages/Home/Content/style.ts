import { COLORS } from 'const';
import styled from 'styled-components';

export const BtnCreateNews = styled.div`
  width: 100%;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 20px;
`;

export const IconCreateNews = styled.div`
  padding: 24px;
  flex: 1;
  border-left: 1px solid ${COLORS.BORDER};
`;
