import { Block as _Block } from 'components';
import styled from 'styled-components';

export const Block = styled(_Block)`
  flex-direction: column;
  padding: 20px;
`;

export const SettingsBlock = styled(Block)`
  margin-bottom: 15px;
`;
